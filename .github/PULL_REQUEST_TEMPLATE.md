## What does this change?

<!-- One or two sentences. Link the issue: Closes #123 -->

## How did you test it?

<!-- e.g. new unit test in packages/core/test, tried it in Chrome with the extension loaded -->

## Checklist

- [ ] `npm run check` passes (lint, format, unit tests, build)
- [ ] New behaviour has tests (engine or backend rules need a test in `packages/core/test`)
- [ ] If this touches blocking, PIN, Failsafe or the agent: I thought about how it could be used to get around a block
- [ ] UI text is plain and friendly, with no em dashes
- [ ] CHANGELOG.md updated under "Unreleased" (for user-visible changes)
