# API Contract
Label: wayfinder:task
Blocked by: DB Schema & Migrations, Backend Service Architecture

## Question
Produce the full localhost REST API contract: every endpoint (method, path, request/response body, auth requirement) for rules CRUD (with active-vs-inactive PIN logic), tasks/subtasks CRUD, Failsafe flow (start/confirm/PIN/cooldown/final-confirm/cancel), Focus Mode (start/stop with type-to-confirm), analytics/accountability-history reads, recovery-code/PIN-reset flow, export/import.
