# Installer/Packaging & Cross-Platform Spec
Label: wayfinder:task
Blocked by: Backend Service Architecture

## Question
Produce the per-OS installer spec: hosts-file paths (Windows/macOS/Linux), OS auto-start registration method per OS (Task Scheduler/launchd/systemd), Start Menu/Applications-folder shortcut creation for Emergency Help, standalone recovery-script packaging (zero-dependency, self-gated), app-data directory location per OS for DB persistence across uninstall.
